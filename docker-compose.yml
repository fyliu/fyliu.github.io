version: "3.9"
services:
  mkdocs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mkdocs
    command: mkdocs serve -a "0.0.0.0:8001"
    ports:
      - "8001:8001"
    volumes:
      - .:/app
